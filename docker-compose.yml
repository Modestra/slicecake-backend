version: "3.9"
networks:
  backend:
    driver: bridge
services: 
  app:
    container_name: backend
    build:
      context: .
      dockerfile: Slicecake-backend/Dockerfile
    ports:
      - 5000:80
    networks:
      - backend