version: "3.9"
networks:
  backend:
    driver: bridge
services: 
  app:
    container_name: backend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    networks:
      - backend